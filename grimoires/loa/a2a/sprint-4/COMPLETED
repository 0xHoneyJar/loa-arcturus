Sprint 4 (FINAL) — Acceptance Harness, Proof-Schema Seam & E2E Validation — COMPLETED

Verdict: APPROVED (independent review + independent security/quality audit; both
         APPROVE/APPROVED after the single shared finding F-1 was resolved).
Date: 2026-06-11
Epic: loa-arcturus-vmz
Tasks closed: loa-arcturus-{2un,uci,aak,2w0,9us} (4.3, 4.4, 4.5, 4.6, 4.E2E).
              4.1 + 4.2 were already done by a prior partial run (found intact,
              built upon, live-exercised — NOT recreated).

Cycle: resume (prior run died on a transient API error mid-4.3) → implement
       (reviewer.md) → review (engineer-feedback.md: APPROVE-WITH-CONCERNS →
       APPROVE) → audit (auditor-sprint-feedback.md: APPROVED-WITH-FINDINGS →
       APPROVED) → COMPLETED.

THE ACCEPTANCE HARNESS DELIVERED (FR-6): an external party boots the repo with
ONE command — `docker compose up` — with ZERO 0xhoneyjar secrets and ZERO keys,
and watches it auto-migrate (0001→0005), seed through the REAL settle() path
(NOT direct inserts), and self-grade PASS / exit 0 in keyless mock mode.
`pnpm verify` prints a per-service Raw/Qualified/flagged/score table + the four
machine-checkable assertions with honest exit-code semantics.

THE PROOF-SCHEMA SEAM DELIVERED (FR-7): docs/PROOF-SCHEMA.md specifies the proof
object {tx_hash, chain_id, from, amount, nonce} field-by-field, the MANDATORY
client-signed-payer / relayer-masking constraint (the load-bearing one handed to
the rail owner — if the rail broadcasts via a managed relayer, on-chain `from`
becomes the relayer, the funding graph collapses, and Qualified Revenue becomes
indistinguishable from Raw), and the fee-deferred/out-of-scope statement.

LIVE VERIFICATION (actually executed this run, NOT deferred):
  * `docker compose up` (rebuilt image): db+redis healthy; migrate 0001→0005
    exit 0; oracle (seed && verify) EXIT 0. Torn down with `down -v`, no
    lingering containers.
  * `pnpm verify` (mock) REAL output:
      - svc-honeyjar   raw $100.000000  qualified $20.000000  flagged $80.000000  score 0.2000
      - svc-thirdparty raw $200.000005  qualified $100.000000 flagged $0.000000   score 0.5000
      - [A] PASS farm excluded   [B] PASS clean survives
      - [C] TESTER-REQUIRED (mock synthetic hashes, honestly not faked)
      - [D] PASS deterministic   → VERDICT PASS, exit 0
    Scores match the Sprint-3 live numbers exactly (0.20 / 0.50).
  * Re-seed idempotent (6 already-present, re-run safe). Verify byte-identical
    across two SEPARATE processes (sha256 match) → Assertion D holds cross-process.
  * CI YAML (.github/workflows/acceptance.yml) valid + keyless (permissions:
    contents: read; zero ${{ secrets.* }}); its migrate→seed→verify commands are
    the same ones proven to exit 0 locally.

TWO REAL DEFECTS FOUND + FIXED (surfaced the FIRST time the JS settle() path ran
against a live Postgres — Sprints 1-3 exercised only the insert SHAPES via psql):
  1. community-scope.ts: `SET LOCAL app.community_id = $1` → `set_config(
     'app.community_id', $1, true)`. Postgres rejects bind params in SET LOCAL
     (`syntax error at or near "$1"`); set_config(is_local=true) is the canonical,
     parameterizable, transaction-local equivalent. Verified against live DB.
  2. x402-settlement.ts: `RETURNING id` → `RETURNING event_id` on the usage_events
     insert (canonical PK name per migration 0004 / upstream 0011; OPEN-1 drift).
Both are minimal, dated AGPL §5(a) change notices, economic logic untouched
(oracle unit tests 10/10 still pass).

POST-REVIEW CORRECTION (F-1): both independent gates flagged the SAME single
substantive finding — verify.ts's isCounted() docstring claimed a per-service
Σ(counted)==qualified_micro anti-drift cross-check that was not implemented.
RESOLVED by implementing the promised safeguard (reviewers' preferred option):
the equality is now computed in the Assertion-A loop and FAILS the run (exit 1)
on classify-mirror drift. Re-verified PASS / exit 0 / determinism intact.

E2E (Task 4.E2E, P0) — all 5 goals validated with real evidence
(grimoires/loa/a2a/sprint-4/e2e-evidence.md):
  G-1 ACHIEVED  payer_address persisted = on-chain `from`, 6/6, 0 NULL.
  G-2 ACHIEVED  only chain_id 80069; no hardcoded base/USDC in any data path.
  G-3 ACHIEVED  scores 0.20 / 0.50; Assertions A & B pass; score ∈ [0,1].
  G-4 ACHIEVED  clean `docker compose up` → oracle exit 0, keyless; CI keyless.
  G-5 ACHIEVED  seam + relayer-masking constraint + fee-deferred all present.

PROVENANCE HONESTY (the product): seed writes via real settle() (no faked
tx_hashes); Assertion C honestly tester-required in mock (never faked to resolve);
README makes NO live-economy claim, mechanism-not-economy disclaimer is its FIRST
paragraph; HONEY token standard stays UNVERIFIED; affiliate allowlist is
clearly-labeled unmatchable placeholders; relayer-masking handed to the rail owner.

SCOPE: read-only / fee-free / PoL-free preserved; Role 1 OUT. No git
staging/commit/push/PR — human stages. ../loa-freeside untouched. New files
untracked; `br` beads writes only.

PoC STATUS: COMPLETE. This is the FINAL sprint. The acceptance harness, proof-
schema seam, and E2E validation are all delivered and live-verified green
(keyless). The ONLY remaining items are TESTER-REQUIRED by design — Assertion C's
live explorer leg and `dune`-mode A/B on real addresses, which need a tester's own
faucet-funded throwaway key / Dune Sim key. Both code paths are implemented and
documented; only the live execution is deferred to a tester. STOPPING here.
