# Agent Working Memory (NOTES.md)

> This file persists agent context across sessions and compaction cycles.
> Updated automatically by agents. Manual edits are preserved.

## Active Sub-Goals
<!-- Current objectives being pursued -->
- PLANNING COMPLETE (2026-06-10): PRD + SDD + Sprint Plan + beads materialized for the Role-3 Proof-of-Revenue Oracle PoC.
- **SPRINT 1 IMPLEMENTED (2026-06-10)** via `/implement sprint-1`. All 5 tasks + epic `loa-arcturus-mk0` CLOSED in beads. Deliverables: LICENSE/NOTICE/CONTRIBUTING, 11 verbatim-copied `.ts` files + trimmed `core/ports/index.ts`, 4 reconciled migrations (0001-0004) + `migrations/PROVENANCE.md`, `package.json`/`tsconfig.json`/`DEPENDENCY-PINNING.md`, `migrations/run-migrations.sh`. Report at `grimoires/loa/a2a/sprint-1/reviewer.md`.
- **RESIDUAL on Sprint 1 — NOW CLEARED (2026-06-10):** the clean-DB run that Sprint 1 deferred was executed during Sprint 2's live verification. Migrations 0001→0004 (and now 0005) apply GREEN on a real Postgres 16. Sprint 1 AC3/AC4 are effectively satisfied.
- **SPRINT 2 IMPLEMENTED + REVIEWED + AUDITED (2026-06-10)** via the `/run sprint-2` implement→review→audit loop. Epic `loa-arcturus-ict` + tasks `{l7u,bpr,axx,nz5,yuv}` (2.1–2.5) CLOSED in beads. Verdict APPROVED. Artifacts in `grimoires/loa/a2a/sprint-2/` (reviewer.md, engineer-feedback.md, auditor-sprint-feedback.md, COMPLETED).
  - **THE GAP CLOSED (FR-3):** `migrations/0005_x402_settlements.sql` (Option B) + `settle()` now inserts `payer_address=proof.from` (Step 3b, inside the withCommunityScope txn). `payload='{}'` discard gone from the live path.
  - **LIVE DB VERIFIED (not deferred):** full 0001→0005 chain GREEN on throwaway Postgres 16; UNIQUE(chain_id,tx_hash) rejected a duplicate live; append-only triggers blocked UPDATE/DELETE live; full settle() insert path ran end-to-end. Containers torn down. `run-migrations.sh` extended to 0005 + smoke re-run exit 0.
  - **FR-4:** Bepolia 80069 added to CHAIN_CONFIGS (BERA/18/`https://bepolia.rpc.berachain.com`/`https://bepolia.beratrail.io`) — **OPEN-6 CONFIRMED** against the ethereum-lists EVM chain registry (chainid.network/chains.json, chainId 80069). Mainnet 80094 retained. Facilitator config-driven (`chainKey`/`settlementToken`); zero base/USDC/8453 literals on the facilitator path.
  - **OPEN-2 still UNVERIFIED (by design):** HONEY token standard / Bepolia contract NOT guessed; recorded as confirm-against-source task in `docs/SETTLEMENT-MODEL.md`. `settlementToken` is a display symbol string only.
- **SPRINT 3 IMPLEMENTED + REVIEWED + AUDITED (2026-06-11)** via the `/run sprint-3` implement→review→audit loop. Epic `loa-arcturus-6ip` + tasks `{208,itm,lun,amp,he5,chu,fdc}` (3.1–3.7) CLOSED in beads. Verdict APPROVED. Artifacts in `grimoires/loa/a2a/sprint-3/` (reviewer.md, engineer-feedback.md, auditor-sprint-feedback.md, COMPLETED).
  - **THE ORACLE DELIVERED (FR-5):** net-new `src/oracle/` emits per service `{raw_micro, qualified_micro, score∈[0,1], flagged_affiliated_micro, methodology}` from `x402_settlements` + `IChainProvider`. All 5 §5 filters: affiliate-exclusion (flag-not-count), dust floor, circular-flow cycle detection (NET-NEW, `funding-graph.ts`), wallet-age weighting, external-origin bonus. Filters 1-3 binary gates → headline; 4-5 diagnostics only.
  - **LIVE DB VERIFIED:** migrations 0001→0005 green on throwaway Postgres 16; `verify-live.ts` → svc-honeyjar raw $100/flagged $80/qualified $20/**score 0.20** (ANTI-SELF-GRADING proven — affiliated revenue flagged + excluded), svc-thirdparty **score ≈0.50** (circular farm $100 + dust $0.000005 dropped; 2 clean payers $40+$60 survive → FARM-REDUCES/CLEAN-SURVIVES). Determinism: byte-identical + identical sha256 across two separate processes. UNIQUE + append-only guards observed live. Container torn down.
  - **OPEN-3/4/5 resolved HONESTLY:** OPEN-3 affiliate allowlist config-driven w/ clearly-labeled placeholders (no invented address); OPEN-4 dust floor $0.01 documented tunable; OPEN-5 weighting-vs-binary reconciliation written down (headline = unweighted binary-gate ratio; 4-5 diagnostics) → reproducible. Wrote `docs/ORACLE-METHODOLOGY.md` (also closed the dangling `config.ts:17` reference).
  - **2 INFO findings (non-blocking):** `Number()` precision ceiling ≈$9B/service on score divide; unnecessary `SET LOCAL app.community_id` on RLS-stripped table.
  - **STOPPED at Sprint 3** per run scope. Sprint 4 NOT started (no docker-compose, seed/verify scripts, CI, README quickstart, or PROOF-SCHEMA.md). Whole-project `tsc`/`test` redness is pre-existing copied adapters (`hkj`, deferred P2) + missing vitest config — NOT Sprint-3 code (oracle subset typechecks clean, 10/10 oracle tests pass).
- **NEW follow-up `loa-arcturus-7bi` (Sprint 4):** settle() persists client-asserted chain_id/tx_hash; the server must READ the chain to confirm the tx before the oracle counts it (SDD §5.4 / Assertion C). Out of Sprint 2 scope. Surfaced in sprint-2 engineer-feedback.md concern #1.
- Next action (FUTURE run, NOT this one): Sprint 3 (The Oracle — ruleset + realness score). Sprint 2 was the scope of this run; Sprint 3 NOT started.
- Git: NO staging/commit/push/PR performed (human stages). Only `br` tracker writes + app files under ~/loa-dev/loa-arcturus. ../loa-freeside treated strictly read-only (confirmed 0 dirty files).

## Discovered Technical Debt
<!-- Issues found during implementation that need future attention -->
- **OPEN-1 (RESOLVED in Sprint 1, STRIP-DOWN) — copied migrations do NOT stand alone.** Resolution recorded in `migrations/PROVENANCE.md`. Net-new findings beyond the planning note: (a) the **runtime** settle() path depends on upstream **0012** `app.insert_lot_entry_fn()` (called by `lot-entry-repository.ts:110`) + the `lot_entries` columns 0012 adds (`correlation_id/purpose/sequence_number/causation_id`) — 0012 is NOT in spec §2; (b) roles `arrakis_app`/`arrakis_admin` are created by **NO** migration (external infra), so their GRANT/REVOKE lines were stripped; (c) a **SECOND** code↔schema drift: `verifyNonceUnique()` writes `webhook_events.event_type` which upstream 0010 lacks (fixed by adding `event_type TEXT`). usage_events drift (source/reference_id absent; nft_id/pool_id NOT NULL) reconciled to the shape settle() writes. crypto_payments (NOWPayments leave-behind) bundled in upstream 0010 was EXCLUDED.
- **`loa-arcturus-hkj` (discovered Sprint 1) — chain-adapter sibling closure deferred to Sprint 3.** The copied chain adapters import siblings NOT in §2 (`dune-sim-types.js`→zod, `native-reader.js`→viem+opossum, `config.js`, `dune-sim-metrics.js`, `metrics.js`). Not on the settle() path (Sprint 1's gate), so the §2-named adapter files are present verbatim w/ headers but their full compile-closure is deferred to the oracle sprint. Add zod^3.23.0/viem^2.21.0/opossum^9.0.0 then (sources in `DEPENDENCY-PINNING.md`).
- **OPEN-5 — filter formula ambiguity.** Spec §5 defines score = Qualified/Raw (a Σ of passing `actual_micro`) but describes filters 4–5 as *weighting*, not binary pass/drop. Reconcile + document in Sprint 3 Task 3.5; headline must stay reproducible.

## Blockers & Dependencies
<!-- External factors affecting progress -->
- **THE GAP blocks everything downstream** (spec §3, line 82): payer persistence (Sprint 2 Task 2.1/2.2) gates the oracle (Sprint 3) and harness (Sprint 4). Wired in beads: `loa-arcturus-208` (oracle skeleton) is blocked by `bpr` (settle persist) which is blocked by `l7u` (settlements table) which is blocked by `2ri` (clean-DB migrations green).
- **OPEN-2 / UNVERIFIED — HONEY token standard.** No EIP is sourced; only "Honey and USDC have the functionality required for x402" (spec §4/§11). Self-broadcast model avoids dependence. Confirm HONEY's Bepolia contract + methods against Berachain docs BEFORE assuming any transfer-with-authorization flow. Carried as Sprint 2 Task 2.5 — must NOT be resolved by guessing.
- **OPEN-6 — Bepolia 80069 not in CHAIN_CONFIGS** (verified `../loa-freeside/packages/core/ports/chain-provider.ts:398-459`). Must add a new `ChainConfig` (RPC + explorer `https://bepolia.beratrail.io`); confirm native symbol/decimals + public RPC list against Berachain docs (Sprint 2 Task 2.3).
- **Anti-sybil depends on Dune Sim.** `getActivity`/`getBalanceWithUSD` are optional/Dune-Sim-exclusive (`chain-provider.ts:278`). MockChainProvider (Sprint 3 Task 3.7) is the keyless fallback so external testers are never blocked.
- **OPEN-3 / OPEN-4** — affiliate allowlist seed + dust-floor threshold needed for Sprint 3 filters; values not in spec.
- External: ../loa-freeside is the read-only AGPL source. Bepolia faucet (bepolia.faucet.berachain.com) + public RPC must remain available to testers.

## Session Continuity
<!-- Key context to restore on next session -->
| 2026-06-11T~12:00Z | Loa /run sprint-4 (RESUME) | Resumed Sprint 4 (FINAL) after a prior partial run died on a transient API error mid-Task-4.3. 4.1 (docker-compose+.env.example+auto-migrate) and 4.2 (seed-bepolia.ts) found INTACT — built upon, NOT recreated. COMPLETED 4.3 (scripts/verify.ts — per-service table + Assertions A-D + exit codes; added seed:bepolia/verify to package.json), 4.4 (.github/workflows/acceptance.yml — keyless mock, zero secrets), 4.5 (README.md — mechanism-not-economy disclaimer FIRST paragraph), 4.6 (docs/PROOF-SCHEMA.md — seam {tx_hash,chain_id,from,amount,nonce} + load-bearing client-signed-payer/relayer-masking constraint + fee-deferred statement). FOUND+FIXED 2 REAL DEFECTS surfaced the first time the JS settle() path ran live: (1) community-scope.ts `SET LOCAL app.community_id=$1` → `set_config('app.community_id',$1,true)` (PG rejects bind params in SET LOCAL); (2) x402-settlement.ts `RETURNING id` → `RETURNING event_id` (canonical PK, OPEN-1 drift). Both dated AGPL §5(a) notices; economic logic untouched (oracle tests 10/10). LIVE: `docker compose up` (rebuilt) booted healthy, auto-migrated 0001→0005, oracle seed&&verify EXIT 0; `pnpm verify` mock → svc-honeyjar 0.20 / svc-thirdparty 0.50, A✓ B✓ C◐(tester-required, synthetic mock: hashes NOT faked) D✓, exit 0; re-seed idempotent; verify byte-identical across 2 processes; CI YAML valid+keyless. Torn down clean. E2E G-1..G-5 all ACHIEVED with real evidence (a2a/sprint-4/e2e-evidence.md). GATE: 2 INDEPENDENT reviewers (review+audit) → APPROVE/APPROVED; both flagged SAME finding F-1 (verify.ts docstring claimed an isCounted Σ==qualified_micro cross-check that wasn't implemented) → RESOLVED by implementing the promised per-service equality (fails run on classify-mirror drift), re-verified PASS/exit 0. Artifacts: a2a/sprint-4/{reviewer,engineer-feedback,auditor-sprint-feedback,e2e-evidence}.md + COMPLETED. Closed epic vmz + tasks {2un,uci,aak,2w0,9us}. `loa-arcturus-7bi` (on-chain tx_hash confirm / Assertion C live leg) INTENTIONALLY LEFT OPEN — tester-required (needs faucet key), out of keyless-PoC scope, documented. NO git ops; ../loa-freeside untouched. **PoC COMPLETE — FINAL sprint done; only tester-required live legs (Assertion C + dune A/B) remain by design.** (Beads health shows DEGRADED = pre-existing benign stale blocked_issues_cache; db==jsonl in sync, 30 records, write-probe OK — not a Sprint-4 issue.) |
| Timestamp | Agent | Summary |
|-----------|-------|---------|
| 2026-06-11T~11:50Z | Loa /run sprint-3 | Executed Sprint 3 (The Oracle) via implement→review→audit. Found Sprint-3 code already on disk from a prior partial run (task 3.1/epic in-progress, 3.2-3.7 still open, no verification run). Verified it: net-new src/oracle/ (types, settlements-repo read-only, oracle.ts computeServiceRevenue/computeAllServices, config.ts OPEN-3/4/5 env-tunable, funding-graph.ts NET-NEW cycle detection, mock-chain-provider.ts keyless deterministic, synthetic-graph.ts, oracle.test.ts 10/10, verify-live.ts). Wrote MISSING docs/ORACLE-METHODOLOGY.md (config.ts:17 ref now resolves). RAN LIVE on throwaway Postgres 16: 0001→0005 green; svc-honeyjar score 0.20 ($80 flagged+excluded = ANTI-SELF-GRADING), svc-thirdparty ≈0.50 (farm+dust dropped, clean survive); byte-identical determinism + identical sha256 across 2 processes; UNIQUE + append-only guards live. Sprint-3 oracle typechecks CLEAN (whole-project redness = pre-existing copied adapters hkj + missing vitest config). Review: All good w/ non-blocking concerns. Audit: APPROVED (single-model; flatline off). Closed epic 6ip + 208/itm/lun/amp/he5/chu/fdc. 2 INFO findings. NO git ops; ../loa-freeside untouched (0 dirty). STOPPED at Sprint 3 — Sprint 4 NOT started. |
| 2026-06-10T18:25:30Z | Loa /plan (planning-only run) | Read ROLE3-ORACLE-SPEC.md (authoritative). Audited 6 spec claims against ../loa-freeside source — ALL verified (payer discarded, base/USDC hardcoded, Bepolia absent, getActivity Dune-exclusive). Wrote prd.md, sdd.md, sprint.md. Initialized beads (`br init` → HEALTHY), created 4 epics + 24 tasks, wired critical-path deps (no cycles), flushed to .beads/issues.jsonl. Created ledger.json (cycle-001, 4 sprints). Surfaced OPEN-1 (migration dependency + usage_events drift — net-new finding beyond spec §2) and OPEN-5 (filter weighting ambiguity). NO code/migrations/copies/git ops performed. |
| 2026-06-10T~22:00Z | Loa /run sprint-2 | Executed Sprint 2 (THE GAP + Bepolia) via implement→review→audit. Authored migration 0005 x402_settlements (Option B: payer_address, UNIQUE(chain_id,tx_hash), payer/agent indexes, append-only triggers, >=0 CHECKs). Modified settle() to persist proof.from inside the existing txn (Step 3b). Added Bepolia 80069 to CHAIN_CONFIGS (OPEN-6 confirmed vs ethereum-lists registry chainid.network: BERA/18/bepolia.rpc.berachain.com/bepolia.beratrail.io); made x402.routes facilitator config-driven (chainKey/settlementToken), removed base/USDC/8453. Wrote docs/SETTLEMENT-MODEL.md (self-broadcast model + HONEY UNVERIFIED confirm task OPEN-2). RAN LIVE: 0001→0005 green on throwaway Postgres 16; duplicate (chain_id,tx_hash) rejected; append-only UPDATE/DELETE blocked; full settle() path e2e; containers torn down. Updated copied-file AGPL §5(a) notices verbatim→dated. Closed beads epic ict + tasks l7u/bpr/axx/nz5/yuv; filed 7bi (tx_hash confirmation→Sprint 4). Audit APPROVED. NO git ops; freeside untouched. Sprint 3 NOT started. |
| 2026-06-10T~19:00Z | Loa /implement sprint-1 | Implemented Sprint 1 (scaffold + curated copy). Source loa-freeside@f0354ff (read-only). Created LICENSE (AGPL, commercial line removed), NOTICE, CONTRIBUTING (DCO); copied 11 .ts files VERBATIM (md5-verified) w/ AGPL §5(a) provenance headers + trimmed core/ports barrel (excl agent-gateway leave-behind); authored migrations 0001-0004 (STRIP-DOWN resolution of OPEN-1) + run-migrations.sh; pinned deps vs real sietch/adapters/core manifests; PROVENANCE.md/migrations/PROVENANCE.md/DEPENDENCY-PINNING.md. Static cross-check of settle() path GREEN. Clean-DB live run DEFERRED (no Postgres in env) — exact command documented, NOT faked. All 5 tasks + epic closed in beads. Net-new findings: 0012 runtime dep, roles-not-in-migration, webhook_events.event_type 2nd drift, chain-adapter closure (loa-arcturus-hkj→Sprint 3). NO git ops. |

## Decision Log
<!-- Major decisions with rationale -->
- **Plan follows spec §8 build order exactly** → 4 sprints: (1) scaffold+copy, (2) THE GAP + Bepolia, (3) oracle+score, (4) harness+seam+E2E. Rationale: §8 is the spec's own canonical sequence; FR-3 explicitly blocks downstream.
- **Adopt payer-persistence Option B** (`x402_settlements` table) over Option A (JSONB in `webhook_events.payload`). Rationale: oracle queries by `payer_address` and `(agent_id, window)`; Option A forces JSON extraction on every read. Spec marks B "recommended for the oracle" (§3).
- **Strip-down preferred for migration resolution** (drop RLS/tenant machinery) over porting `0001–0008` verbatim. Rationale: PoC is single-tenant + read-only; tenant isolation is loa-freeside product surface, not meter mechanism (simplicity-first). ~~DECISION DEFERRED~~ → **EXECUTED in Sprint 1** (`migrations/PROVENANCE.md`): ported prerequisite functions (app schema + current_community_id + prevent_mutation + update_lot_status + insert_lot_entry_fn), KEPT append-only triggers (the real integrity mechanism), STRIPPED RLS policies + FORCE RLS + role GRANTs + the 0012 REVOKE (roles don't exist on clean DB). Append-only invariants survive; only multi-tenant isolation (unused by PoC) dropped.
- **License: pure AGPL-3.0 for arcturus** (Sprint 1). Sourced canonical AGPL text from `../loa-freeside/LICENSE.md` (662L) minus the single freeside-specific commercial dual-license line (L11). Rationale: the dual-license offer is freeside's to make as copyright holder; a CLA for arcturus is later/optional per spec §2:57-58. Provenance in PROVENANCE.md.
- **Per-file "headers intact" = preserve existing JSDoc + prepend §5(a) provenance block.** Source .ts files carry descriptive JSDoc, NOT SPDX/AGPL per-file headers (verified zero license tokens). Did NOT fabricate AGPL header lines that never existed — that would itself be provenance dishonesty. AGPL coverage = repo LICENSE + NOTICE + per-file provenance pointer.
- **Role 3 and Role 1 kept separate; PoC is read-only/fee-free/PoL-free.** §8.8 (realness→PoL gating) explicitly OUT of scope. No on-chain fee/treasury logic anywhere.
- **Self-broadcast settlement model** (no managed facilitator/thirdweb/relayer). Rationale: keeps on-chain `from` == real payer, the anti-sybil trust anchor (spec §4/§10.1/§11). Relayer-masking constraint handed to rail owner in PROOF-SCHEMA.md.
- **Provenance honesty preserved:** UNVERIFIED items (HONEY standard) carried forward as confirm-against-source tasks, never resolved by guessing — this is an anti-fraud project, the meter's credibility IS the product.

## Beads Reference (cycle-001)
| Sprint | Epic ID | Theme |
|--------|---------|-------|
| 1 | loa-arcturus-mk0 | AGPL Scaffolding & Curated Copy |
| 2 | loa-arcturus-ict | The Gap (Payer Persistence) + Bepolia Config |
| 3 | loa-arcturus-6ip | The Oracle — Ruleset & Realness Score |
| 4 | loa-arcturus-vmz | Acceptance Harness, Proof-Schema Seam & E2E |

Critical path (blocks): `2ri`(1.4 migrations green) → `l7u`(2.1 table) → `bpr`(2.2 settle persist) → `208`(3.1 oracle) → `chu`(3.6 score) → `2un`(4.3 verify) → `9us`(4.E2E).
